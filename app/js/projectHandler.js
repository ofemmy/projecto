let ProjectHandler = {
  init({ title, description, status, percent }) {
    this.title = title;
    this.description = description;
    this.status = status;
    this.percent = percent;
  },
  update() {},
  delete() {},
};
